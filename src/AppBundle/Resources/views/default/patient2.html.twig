<!DOCTYPE html>
<html>

    <head>
        <meta content="text/html; charset=utf-8" http-equiv="Content-Type">
        <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet" type="text/css"/>
    </head>

    <body>
        <div class="container">
                <div class="form-group">
                    
                    {{form_start(form)}}
                    
                    {#{{form_row(myform.lastname)}}
                    {{form_row(myform.fatherName)}}
                    {{form_row(myform.nationalCode)}}
                    {{form_row(myform.gender)}}
                    {{form_row(myform.birthday)}}
                    {{form_row(myform.email)}}
                    {{form_row(myform.insurance)}}
                    {{form_row(myform.telphone)}}
                    {{form_row(myform.mobile)}}#}
                    <input type="submit" value="send"/>
                    {{form_end(form)}}
                </div>
        </div>
    </body>

</html>
